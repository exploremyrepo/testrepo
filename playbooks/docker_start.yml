---

- name: Build a web app on docker
  hosts: ansible
  become: true
  
  tasks:
   - name: Start the container
     community.docker.docker_container:
        name: thirsty_feistel
        image: ubuntu:latest
        state: started
        command: sleep infinity
